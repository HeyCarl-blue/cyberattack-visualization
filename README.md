TODO:

1. Collision Detection per ogni particella (con sistema di ottimizzazione) + collision detection sul server.
2. Sistema di timeout per emulare la processazione dei pacchetti che passano nella bocca del server e del ttl dei pacchetti che sono al di fuori
3. Customizzazioni di grafica (colore del tipo dei pacchetti) -- Rivedi parametri dell'engine delle particelle.